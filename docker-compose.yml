# Read-only MCP server over the local vault directory.
# Usage:
#   docker compose up --build
# Vault is mounted read-only; disposable FTS index is written outside the mount.
services:
  mcp:
    build: .
    image: evidence-vault-mcp:local
    environment:
      EVIDENCE_VAULT_ROOT: /vault
      EVIDENCE_VAULT_INDEX_PATH: /tmp/evidence-vault-index.sqlite
    volumes:
      - type: bind
        source: .
        target: /vault
        read_only: true
    ports:
      - "8000:8000"
    command:
      [
        "uv",
        "run",
        "--offline",
        "--no-sync",
        "evidence-vault",
        "--vault",
        "/vault",
        "mcp",
        "serve",
        "--transport",
        "sse",
        "--host",
        "0.0.0.0",
        "--port",
        "8000",
      ]
