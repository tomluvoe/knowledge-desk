# Sources

Each successful ingest creates `src-<digest-prefix>/` containing one immutable byte-identical original, `manifest.json`, and readable `normalized.md`. Do not hand-edit ingested source directories.
