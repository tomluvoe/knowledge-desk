# Inbox

Place untrusted candidate PDF, Markdown, or text files here. Files in this directory are review-only and are not evidence until `evidence-vault ingest` publishes validated artifacts under `sources/`.
