# Proposed update queue

Future non-writer agents place proposed canonical changes here for single-writer review. Queue entries are review-only and do not become truth merely by existing.

`evidence-vault explore … --propose` may write JSON proposals here (gap reports, observation stubs, open questions). Apply them only after human/agent review via `observe`, wiki edits, or a future single-writer workflow (#8).
