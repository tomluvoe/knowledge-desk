# Schema examples

These examples are schema-valid illustrations, not canonical evidence. Ecology and history examples deliberately demonstrate that the core observation model has no finance-specific fields.
